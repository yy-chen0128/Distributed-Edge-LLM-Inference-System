# 论文项目代码深度分析：工作栈结构与核心逻辑

> 日期：2026-08-16
> 目的：把与本项目最相关的论文开源实现 clone 到本地（`project/` 下），逐文件分析"代码在工作栈的哪一部分、实现了什么逻辑"，为搭建我们自己的管理调度层提供可参考的实现细节。
>
> 仓库清单（5 个全部就位）：
> - `project/preble/` — Preble（ICLR'25，KV 前缀共享的分布式调度，**最接近我们要做的**）
> - `project/spotserve/` — SpotServe（ASPLOS'24，可抢占实例的动态重并行化 + 渐进迁移）
> - `project/moe-infinity/` — MoE-Infinity（激活感知专家卸载，跨设备 EP）
> - `project/LMCache/` — LMCache（KV 缓存层，我们已在用的存储/传输层）
> - `project/helix/` — Helix（ASPLOS'25，max-flow 放置 + per-request pipeline，**仿真器+真实系统双实现**）

---

## 一、总览：五个仓库在工作栈中的位置

```
┌─────────────────────────────────────────────┐
│ 管理调度层（我们要自研的层）                  │
│   全局路由/放置决策/迁移协调                   │
│   ↑ Preble 是此层的现成参考（global scheduler）│
├─────────────────────────────────────────────┤
│ KV 缓存层                                    │
│   ↑ LMCache（存储/传输，已在用）              │
│   ↑ MoE-Infinity 的 kv_cache_manager（分页）  │
├─────────────────────────────────────────────┤
│ 推理引擎层                                    │
│   ↑ vLLM（Preble 通过 SSH/URL 驱动的后端）    │
│   ↑ Helix 的 llm_sys/worker（引擎封装）       │
├─────────────────────────────────────────────┤
│ 专家执行层（MoE 专属）                        │
│   ↑ MoE-Infinity 的 expert_executor + prefetch│
│   ↑ SpotServe 的 global_server（层迁移）      │
└─────────────────────────────────────────────┘
```

**工作栈分层逻辑**：越往上越"决策"，越往下越"执行"。Preble 是最上层的"调度大脑"，LMCache 是"缓存+传输手"，MoE-Infinity 是"专家执行肌肉"（跨设备），SpotServe 是"状态迁移引擎"（节点变化时重排）。

---

## 二、Preble（`project/preble/`）—— 缓存感知分布式调度

### 2.1 代码在工作栈的位置
Preble 是一个**独立的调度层（standalone）**，构建在 vLLM/SGLang 之上，通过 OpenAI API 驱动它们。它不做模型推理，只做"请求应该发给哪个 GPU 实例"的决策。

### 2.2 架构与文件映射

```
preble/
├── preble/
│   ├── server/server.py              # ★ 全局调度器入口（FastAPI）
│   ├── global_scheduler_with_time.py # ★ E2 调度算法核心
│   ├── data_parallel_request_cache.py # 路由策略层 + 策略枚举
│   ├── model_runtime_manager.py      # ★ 多 GPU 运行时管理
│   ├── vllm_runtime.py               # vLLM 后端（SSH 远程启动）
│   └── global_lru_cache.py           # LRU 缓存（辅助）
├── python/                           # SGLang 后端集成
├── benchmark/                        # 5 个工作负载的实验数据
└── sim.py / eventsim.py              # 队列仿真（测试用）
```

### 2.3 核心逻辑逐文件展开

**① `server/server.py` — 全局调度器入口（请求生命周期）**

这是 Preble 的运行主循环。核心是两个异步协程 + 一个队列：

```python
# process_req(): FastAPI 接收请求 → tokenize → 放入队列
obj = await request.json()
generate_req_input.input_ids = tokenizer.encode(generate_req_input.text)  # tokenize
await runtime_request_queue.put((obj, request_id))  # 进调度队列

# process_runtime_selection(): 后台协程，从队列取请求 → E2 决策
runtime_id = request_router.select_runtime(
    text, input_ids, sampling_params,
    runtime_id_with_highest_hit_rate=highest_idx,  # 命中率最高的 GPU（exploit）
    hit_rates=hit_rates,
)
# 决策结果写回 runtime_events，主请求流知道去哪个 runtime
```

**工作栈位置**：这是**全局调度器**（管理调度层）的入口。它做了三件事：① tokenize（用 HF tokenizer）；② 调 E2 决策；③ 把请求转发给选中的 vLLM/SGLang 实例并流式返回。**这个 `runtime_request_queue` + 后台决策协程的模式，就是我们自研调度层的骨架。**

**② `global_scheduler_with_time.py` — E2 算法核心**

这是 Preble 的灵魂。两个类：

**`SlidingWindowHistogram`**（滑动窗口直方图）：维护时间窗 H 内每个前缀树节点的访问统计。核心数据结构：
```python
self.histogram[node] += leaf_node.context_length   # 节点被多少 token 命中
self.node_to_count[node] += 1                       # 节点被多少请求共享
self.hit_tokens[node] += 命中token数                 # 缓存命中量
self.prev_mis_rates[node] = 1 - hit_tokens/prompt_tokens  # 未命中率
```
每 `window_duration`（3 分钟）淘汰过期条目。**这个"滑动窗口共享统计"是 E2 判断负载和驱逐代价的输入。**

**`GlobalSchedulerWithTime`**：E2 决策主体。核心方法：

```python
def runtime_selector(...):   # E2 主决策
    leaf_node = self.cache.insert(input_ids)  # 插入全局 radix tree
    important_node = self.get_important_node(leaf_node)
    if leaf_node 在重要节点下:   # 前缀已被缓存
        # EXPLOIT：去持有该前缀缓存的 GPU（多 GPU 则选负载轻的）
        gpu_selected = self.get_parent_gpu_allocation(leaf_node)
        runtime_idx = self.calculate_min_load_cost(leaf_node, gpu_selected)
    else:
        # EXPLORE：全集群选负载最轻
        runtime_idx = self.calculate_min_load_cost(leaf_node, all_gpus)
```

**三成本负载计算**（`calculate_min_load_cost`）：
```python
for gpu_id in selected_gpus:
    cost = histogram.current_allocation_per_gpu()[gpu_id]   # L_i: 窗口内已有负载
    if enable_eviction:
        cost += self.virtual_evict_for_routing(leaf_node, gpu_id)  # M_i: 驱逐代价
    # P_i（新请求未命中 prefill）已隐含在 L_i 更新里
```

**驱逐价值公式**（`get_eviction_prefill_cost`）—— 论文 `M_i = Σ PT_j × N_j` 的实现：
```python
def get_eviction_prefill_cost(node, gpu, is_large_node):
    if not is_large_node: return 0   # 小节点驱逐代价忽略
    return self.prev_mis_rates.get(node, 1.0) \      # N: 未命中率（=共享热度）
        * self.node_to_count.get(node, ref_counter) \  # 共享请求数
        * prefill_time(node.num_tokens, node.context_length)  # PT: 重算时间
```
**工作栈位置**：这是**全局调度算法层**。E2 的 exploit/explore 决策 + 三成本模型，是你要复用的核心逻辑——尤其驱逐价值公式，可直接移植到你"节点离开时 KV 迁移优先级"的决策里。

**③ `data_parallel_request_cache.py` — 路由策略层**

定义了可插拔的**路由策略枚举**：
```python
DataParallelRuntimeSelectionPolicy:
    RANDOM / ROUND_ROBIN / LEAST_OUTSTANDING_REQUESTS / CUSTOM

CustomPolicyType:   # 大量实验策略
    ORACLE / GREEDY_LP / BASIC_MEM_SCHEDULER /
    GlobalScheduler / GlobalSchedulerTime / ...
```
`DataParallelRequestRouter` 是统一路由入口，`CustomRuntimeSelector` 是自定义策略接口——**E2 就是通过 CUSTOM 接进来的**。这个"策略枚举 + 统一路由接口"设计，让我们可以像插插头一样替换调度算法。

**工作栈位置**：这是**调度策略的抽象接口层**。我们自研调度层时，可以照这个设计：一个 `RuntimeSelector` 接口 + 多种策略实现，方便对比实验。

**④ `model_runtime_manager.py` — 运行时管理层**

`ModelDetails` 管理一组 GPU 实例（runtime）：
```python
def load_runtimes(...):
    for config in gpu_configs:
        if simulate:  → SimulationRuntime       # 纯仿真
        elif use_ssh: → SSHRuntime              # SSH 远程
        elif url:     → URLRuntime              # HTTP
        elif vllm_config: → VLLMRuntime         # vLLM 专门
        else:         → ExtendedSGLangRuntime   # SGLang
```

**工作栈位置**：这是**调度器与推理引擎之间的适配层**。一个抽象 `EndpointRuntimeInterface` + 多种实现（仿真/SSH/URL/vLLM/SGLang）。**我们的系统正好需要这种抽象：仿真模式（无硬件）+ 真实模式（vLLM worker）切换**。

**⑤ `vllm_runtime.py` — vLLM 后端**

通过 **paramiko SSH** 远程启动 vLLM OpenAI API server：
```python
command = f'setsid env CUDA_VISIBLE_DEVICES={gpu} {python} -m vllm.entrypoints.openai.api_server ...'
channel.exec_command(command)   # SSH 远程启动
self.url = f"http://{hostname}:{port}"   # 用 HTTP 驱动
```
**工作栈位置**：这是**引擎启动适配层**。Preble 不做任何 vLLM 内部修改，纯靠 OpenAI API 驱动——这就是"复用 vLLM 不动它的内部"的范例。

**⑥ `benchmark/` — 实验工作负载（18 种）**

Preble 的实验覆盖论文的 5 种核心用例 + 更多：
```
tool use 类：react/ (ReAct agent), tip_suggestion/, generative_agents/
agent 类：  multi_chain_reasoning/, tree_of_thought_v0/, tree_of_thought_deep/
长文档：    multi_document_qa/, line_retrieval/
多轮对话：  multi_turn_chat/, mtbench/, llm_judge/
其它：      gsm8k/, mmlu/, hellaswag/, json_decode/...
```
每个 benchmark 是独立的 Python 脚本，构造"长且共享 prompt"的负载。**这是我们设计实验时可直接套用的负载模板**——尤其 `multi_turn_chat`（多轮对话=前缀共享）和 `multi_document_qa`（长文档=长 prompt）最贴我们的场景。

### 2.4 对我们的启示
- Preble 证明了**"独立调度层 + OpenAI API 驱动 vLLM"**是可行的最小架构——和我们"复用 vLLM + LMCache、自研调度层"完全一致。
- `runtime_request_queue` 异步调度模式 + 策略枚举 + 运行时抽象，三个设计直接可复用。
- 它的全局 radix tree（哪个 GPU 缓存哪个前缀）数据结构 = 我们要做的"KV 索引"参考。
- `benchmark/` 的 18 种负载模板可直接用于我们的实验设计（尤其 multi_turn_chat / multi_document_qa）。

---

## 三、SpotServe（`project/spotserve/`）—— 动态重并行化 + 渐进迁移

### 3.1 代码在工作栈的位置
SpotServe 是**状态迁移引擎**：当 GPU 实例被抢占/加入时，它重新计算并行策略（重并行化），并生成逐层的迁移指令。它管理的是"模型层 + KV 在设备间的移动"。

### 3.2 架构与文件映射

```
spotserve/
├── elastic-switch/
│   ├── global_server/
│   │   ├── KMmatcher.py      # ★ 二分图匹配（Device Mapper）
│   │   ├── HotSwitch.py      # ★ 渐进迁移的层调度（生成 send/recv/stay）
│   │   ├── Switch.py         # 迁移执行（FasterTransformer 交互）
│   │   └── main.py           # 全局服务入口
│   ├── scheduler/
│   │   ├── scheduler.py      # ★ 主调度器（策略切换 + 迁移协调）
│   │   └── parallel/solver.py # ★ StrategyOptimizer（重并行化求解）
│   └── main.py
├── ParamsClient/             # C++ 参数客户端（逐层加载）
├── FasterTransformer/        # 修改版推理引擎
└── ckpt/                     # checkpoint 生成
```

### 3.3 核心逻辑逐文件展开

**① `scheduler/parallel/solver.py` — 重并行化求解（StrategyOptimizer）**

给定"当前可用节点数 nnodes"，枚举所有可行的 (dp, tp, pp, bsz, M1, M2) 组合，用 profiling 数据估算延迟，选最优：
```python
def solve(prev_strategy, nnodes, max_bsz, min_tpt, ...):
    tp = 1
    while tp <= max_tp:
        for bsz in bsz_sets:        # batch 指数枚举 1,2,4...
            pp = (min_ws + tp - 1) // tp
            while pp * tp <= nnodes:
                latency = get_approx_latency(tp, pp, bsz, M1, M2)
                dp = nnodes // (pp * tp)
                tpt = dp * bsz / latency
                if tpt >= min_tpt: res.append((dp,tp,pp,bsz,...))
```
延迟模型（`get_approx_latency`）：用 profile 的 `init/incr` 时间 + PP 通信项：
```python
t1 = init * factor * (pp + M1 - 1) / M1 + pp_init_comm/M1 * (pp-1)  # prefill
t2 = incr * factor * (pp + M2 - 1) + pp_incr_comm/M2 * (pp-1)        # decode
lat = t1 + t2 * output_seq_len + embed  # 总延迟
```
**工作栈位置**：这是**重并行化决策层**。对应论文的"在线优化器 <1s"。它枚举 (D,P,M,B) 组合找满足吞吐约束的最小延迟——这正是你要的"节点变动后如何调整并行度"的实现。

**② `global_server/KMmatcher.py` — Device Mapper（二分图匹配）**

KMMatcher 是标准 Kuhn-Munkres（匈牙利）最大权匹配实现（O(n³)）。核心 `get_graph_weights` 计算"旧配置的节点 ↔ 新配置的节点"之间的边权 = **可复用参数量**：
```python
def get_graph_weights(old_stages, old_s, new_stages, new_s, ...):
    # 计算两个 (tp,pp) 配置下，同一层在两个节点上的"层重叠"
    layer_intersection = 重叠的层数
    hidden_intersection = 重叠的 hidden 维度（TP 切分后）
    # 边权 = 重叠层数 × (每层DP参数 + 每层TP参数×重叠hidden)
    return layer_intersection * (layer_dp_params + layer_tp_params * hidden_intersection)
```
**工作栈位置**：这是**设备映射决策层**。它回答"新配置下，每个逻辑位置该映射到哪个物理 GPU 最划算"——边权=能复用多少已加载参数（含 KV）。**这是我们"新 GPU 加入时如何分配"的实现参考。**

**③ `global_server/HotSwitch.py` — 渐进迁移的层调度**

`simple_switch` 生成逐层的迁移指令表：
```python
for layer_id in layer_where_old:
    old_ranks = 旧配置持有该层的 ranks
    new_ranks = 新配置持有该层的 ranks
    send_ranks = [新持有但旧未持有的 ranks]   # 需要接收
    stay_ranks = [新旧都持有的 ranks]         # 原地不动
    del_ranks  = [旧持有但新不持有的 ranks]   # 需要释放
    for dst in send_ranks:
        src = 旧持有者
        insert_entry(switch_dict, src, 'send', layer_id, dst)   # 发送指令
        insert_entry(switch_dict, dst, 'recv', layer_id, src)   # 接收指令
```
**工作栈位置**：这是**迁移执行计划层**。把重并行化的结果翻译成"每个 rank 对每一层该 send/recv/stay"的具体指令——即渐进迁移（前段 stage 迁完即启动）的层粒度调度。**对应你 D4（节点离开/加入时的装载卸载）的"迁什么、谁传谁"实现。**

**④ `scheduler/scheduler.py` — 主调度器（策略切换协调）**

`switch_strategy` 是迁移主流程：

```python
def switch_strategy(prev_nodes, cur_nodes, next_strategy, preempted_pipelines, ddl=None):
    # 1. 估算迁移成本（estimate_transfer_cost）
    # 2. 写新策略到共享内存（global server 读取）
    # 3. 等待旧实例完成（state_mem 轮询）
    # 4. 发送 job_done 信号给 param clients
    # ddl 参数 = 被抢占的宽限期 deadline
```
**工作栈位置**：这是**迁移协调中枢**。它把 solver（决策）+ KMmatcher（映射）+ HotSwitch（计划）+ ParamsClient（执行）串起来。含 **ddl（deadline）处理**——对应论文的"宽限期"。

**⑤ `ParamsClient/` — C++ 迁移执行端**

SpotServe 是 **Python 调度 + C++ 执行**的混合架构。ParamsClient 是运行在每台 GPU 上的 C++ 代理：
```cpp
int main(...) {
    mpi::initialize(&argc, &argv);      // MPI 并行初始化
    nccl_param = init_nccl(...);        // NCCL 通信
    tensor_storage = new TensorStorage{...};  // 张量存储管理
    tensor_storage->load_initial_weights(model_path, pp, dp, tp);  // 初始权重加载
    // 主循环：监听 TCP 迁移指令
    while (1) {
        // interact_thread 处理 client_req（含 layer_id, param_idx, param_type）
        tensor_storage->send_metadata(...)          // 发送张量元数据
        tensor_storage->send_buffdata(...)          // 发送张量数据（逐层迁移）
    }
}
```
**工作栈位置**：这是**迁移的执行端**（数据平面）。Python 调度器（global_server）算出"第几层的参数发给谁"，ParamsClient 通过 TCP 实际传输张量。**这印证了"决策在 Python、执行在 C++"的分层——我们的系统如果要高性能迁移，也需要类似的分层。**

### 3.4 对我们的启示
- SpotServe 是**节点变动处理**的最完整实现：重并行化求解（solver）→ 设备映射（KM）→ 层迁移计划（HotSwitch）→ 协调执行（scheduler + ParamsClient）。
- 它的 **ddl 处理**（被抢占宽限期）正是我们边缘场景"节点无预告离开"的对照——边缘没宽限期，所以需要"事前冗余"（我们之前的研究空白）。
- `get_graph_weights` 的"层重叠 + hidden 重叠"公式可直接用于"新 GPU 加入时评估能复用多少已有参数"。
- Python 决策 + C++ 执行的分层架构，是可复用的工程模式。

---

## 四、Helix（`project/helix/`）—— max-flow 放置 + per-request pipeline

### 4.1 代码在工作栈的位置
Helix 是**放置求解器 + 请求调度器**：先用 max-flow/MILP 求出全局模型放置（哪个节点持有哪几层），再在运行时为每个请求构建独立 pipeline 并调度。它是**仿真器 + 真实系统双实现**（`simulator/` 做实验，`llm_sys/` 是真系统）。

### 4.2 架构与文件映射

```
helix/
├── simulator/
│   ├── event_simulator/           # 集群事件仿真器
│   ├── model_manager/             # 模型管理（llama1_30b/llama2_70b 配置）
│   ├── scheduler/
│   │   ├── base_scheduler.py      # 调度抽象基类
│   │   ├── global_maxflow/
│   │   │   ├── scheduler_core.py  # ★ SchedulerCore（调度主流程）
│   │   │   ├── network_flow.py    # ★ FlowGraph（max-flow 图构建）
│   │   │   ├── global_maxflow_scheduler.py  # GlobalFlowScheduler
│   │   │   ├── interleaved_weighted_round_robin.py  # ★ IWRR 在线调度
│   │   │   └── kv_expectation.py  # ★ KV 水位线
│   │   ├── naive_scheduler.py / shortest_queue / swarm   # 对比基线
│   │   └── execution_policy.py
│   └── trace_generator/
└── llm_sys/                       # 真实系统
    ├── maxflow_host.py            # ★ 主机端（MILP 求解 + 在线调度入口）
    ├── worker.py                  # 引擎封装（vLLM 初始化 + 层推理）
    ├── comm/                      # 通信（ZeroMQ）
    ├── engine/                    # 推理引擎
    └── heuristic_host.py          # 启发式放置（对比）
```

### 4.3 核心逻辑逐文件展开

**① `simulator/scheduler/global_maxflow/network_flow.py` — max-flow 图建模**

这是论文"把异构集群建模为带权图"的实现，用 **networkx** 构建流图。每个计算节点拆成 4 个顶点：
```python
def add_compute_node(node_uid, inference_throughput, inbound_nic, outbound_nic):
    #      inbound TP     inference TP      outbound TP
    # _in ------> _start ------> _end ------> _out
    self.flow_graph.add_edge(f"{node_uid}_in", f"{node_uid}_start",
                             capacity=inbound_nic_token_throughput)   # 入向网卡
    self.flow_graph.add_edge(f"{node_uid}_start", f"{node_uid}_end",
                             capacity=inference_throughput)            # 推理吞吐
    self.flow_graph.add_edge(f"{node_uid}_end", f"{node_uid}_out",
                             capacity=outbound_nic_token_throughput)  # 出向网卡

def add_link(prev, next, throughput):
    self.flow_graph.add_edge(f"{prev}_out", f"{next}_in", capacity=throughput)  # 链路
```
`update_flow` 把 source/sink/compute_nodes 全部转成图，**边容量都以 token/s 为单位**（网卡带宽 ÷ token_size）。这就是论文"节点拆分 c_in/c_out 两顶点、边容量=token/激活字节数"的精确实现。

**工作栈位置**：这是**放置建模层**。FlowGraph 把物理集群（节点算力 + 链路带宽）翻译成带容量约束的流图，供 max-flow 求解。

**② `scheduler_core.py` + `global_maxflow_scheduler.py` — 调度主流程**

`GlobalFlowScheduler.schedule(request)` 是逐请求调度入口：在流图上找到 source→sink 的路径（=一个 pipeline），绑定各节点的 IWRR 选择器，构建 `request.mini_pipeline`。`get_schedule`（在 `maxflow_host.py`）把结果翻译成 `compute_node_ids + start_layers + end_layers`——即"这个请求经过哪些节点、每个节点算哪几层"。

**工作栈位置**：这是**请求调度层**。对应论文的 per-request pipeline。

**③ `interleaved_weighted_round_robin.py` — IWRR 在线选择**

```python
class IWRR:
    def choose_one(self, workload, mask):
        # 在未被 mask 屏蔽的候选中，选"加载后负载最小"的
        for cur_idx, (capacity, load, cur_mask) in enumerate(zip(capacities, loads, mask)):
            if cur_mask and load + workload/capacity < best:
                best = cur_idx
        self.loads[best] += workload / capacities[best]
        return best
```
- **mask** 参数 = KV 水位线屏蔽（超水位线的节点不参与选择，见 `kv_expectation.py`）。
- 负载按 `workload / capacity` 归一化（算"占用比例"而非绝对量），跨异构设备可比。

**工作栈位置**：这是**在线路由选择器**。IWRR 在每个 pipeline 节点上选"下一个去哪"，mask 就是 KV 感知的入口。

**④ `kv_expectation.py` — KV 水位线**

`KVExpectation` 预估每个节点的 KV 占用（用平均输出长度 × 请求数），超过高水位线则 mask 掉该节点——防止 KV cache 超订。这对应论文的"KV-cache 水位线 mask"。

**工作栈位置**：这是**KV 容量约束层**。它把"节点能装多少 KV"变成调度时的硬约束——**这个"KV 容量 → 路由 mask"的模式，我们边缘场景要复用**（显存小，KV 约束更硬）。

**⑤ `llm_sys/worker.py` — 引擎封装**

```python
def init_engine(layer_ids, model_name, vram_usage=0.8):  # 按 layer_ids 装载模型分片
def run_and_submit(engine, start_idx, end_idx, ...):      # 推理指定层区间
```
worker 只装载分配到的层区间（`layer_ids`），推理时执行 `[start_idx, end_idx)` 的层。这就是"每个节点持有连续一段 layer"的实现。

**工作栈位置**：这是**引擎执行层**。类似 Preble 的 vLLM 后端，但 Helix 的 worker 是"按层分片装载"（更细粒度）。

### 4.4 对我们的启示
- Helix 的**仿真器 + 真实系统双实现**是极好的架构模板——仿真器做算法实验，真实系统验证。我们可以照做（你的 demo 已是雏形）。
- `FlowGraph` 的图建模 + `IWRR` 在线选择 + `KVExpectation` 水位线，三者组合是"放置→调度→KV约束"的完整链路。
- 它是**静态放置**（跑一次 MILP），不做节点变动——结合 SpotServe 的动态重并行化，就是我们要做的完整系统。
- 真实系统用 vLLM 做引擎（worker.py），和我们的复用策略一致。

---

## 五、MoE-Infinity（`project/moe-infinity/`）—— 激活感知专家卸载

### 5.1 代码在工作栈的位置
MoE-Infinity 是**专家执行层 + 专家缓存管理**：管理"哪些专家在 GPU / 哪些在 CPU / 盘"，按激活频率预取和淘汰。也支持跨设备 EP（PyTorch RPC）。

### 5.2 架构与文件映射

```
moe_infinity/
├── memory/
│   ├── expert_tracer.py        # ★ EAM 追踪器（专家激活矩阵）
│   ├── expert_priority_score.py # ★ 优先级打分（LRU/LFU/激活频率）
│   ├── expert_prefetcher.py    # ★ 激活感知预取
│   ├── expert_entry.py         # 缓存条目
│   ├── kv_cache_manager.py     # KV 分页 + 前缀缓存
│   ├── memory_coordinator.py   # 内存预算分配
│   └── offloading_policy.py    # 缓存驱逐策略（LRU/ARC）
├── distributed/
│   └── expert_executor.py      # ★ 跨设备专家执行（RPC 分发）
├── engine/
│   ├── expert_offload_coordinator.py  # 专家卸载协调
│   └── scheduler.py            # 请求调度 + 前缀缓存
├── kernel/                     # 融合算子（fused_ffn 等）
└── models/                     # 各模型支持（deepseek/mixtral/qwen...）
```

### 5.3 核心逻辑逐文件展开

**① `memory/expert_tracer.py` — EAM 追踪器（论文核心）**

追踪每个请求的专家激活模式：
```python
class ExpertTracer:
    def create_entry(self, seq_id):    # 新请求开始追踪
    def update_entry(self, seq_id, expert_list, layer_idx):  # 记录每层激活的专家
    def finish_entry(self, seq_id):    # 请求完成，固化 EAM
    def find_most_similar(self, matrix, layer_idx):  # 找历史最相似的激活模式
```
**工作栈位置**：这是**专家激活预测层**。EAM 是论文"激活感知"的数据来源——`find_most_similar` 用历史请求的激活矩阵预测当前请求未来会激活哪些专家，从而预取。

**② `memory/expert_priority_score.py` — 专家优先级打分**

实现三种打分 + 混合：
```python
def lru_score(...)   # 最近最少使用
def lfu_score(...)   # 最不常使用（激活频率归一化）
def priority_score(expert_freq, cache_entries, trace_entries, decoder_entry, current_layer, total_layer):
    # 激活频率 + 层衰减加权
    frequency_score[expert_layer, expert_idx] = 激活次数
    # 混合：频率分 × 层衰减（layer_decay 函数，越靠近当前层越优先保留）
```
**工作栈位置**：这是**专家缓存淘汰决策层**。对应论文"激活感知缓存"。**这就是"专家命中"的优先级排序实现——和我们讨论的 KV 优先级排序是同构的**（SMoE 的分数感知淘汰也是类似思路）。

**③ `distributed/expert_executor.py` — 跨设备专家执行（EP）**

这是**真正的跨设备 EP**（PyTorch RPC）：
```python
def dispatch(self, hidden_states, router_mask, layer_id):
    expert_list = 被激活的专家
    device_list = device_map_manager.get_target_device(expert_list)  # 专家→设备映射
    for rank in visited_ranks:
        rpc.rpc_async(f"worker_{rank}", _call_expert_dispatcher,
            args=("set_inputs", hidden_states.cpu(), router_mask.cpu()))  # 发 hidden 到各 rank
    # 各 rank 的 expert_dispatcher 计算 → 结果聚合
```
**工作栈位置**：这是**跨设备专家执行层**。用 PyTorch RPC 把 token 的 hidden state 发给持有激活专家的 worker 计算——这就是 EP 的 dispatch/combine 的工程实现。**注意：它用 RPC + CPU 传输 hidden states，说明 MoE-Infinity 假设网络够快（数据中心）；你的边缘 WiFi 场景要做跨设备 EP 需要更轻量的传输。**

**④ `memory/kv_cache_manager.py` — KV 分页 + 前缀缓存**

和 vLLM PagedAttention 类似的 KV 块管理：
```python
class KVCacheManager:
    def allocate_blocks_for_sequence(...)  # 序列分配 KV 块
    def cache_gpu_block(self, block, block_hash):  # 前缀缓存 hash 索引
    def get_cached_gpu_block(self, block_hash):    # 按 hash 查缓存
    def prepare_swap_out(self, seq_id):            # 准备换出
```
**工作栈位置**：这是**KV 存储层**。用 block_hash 做前缀缓存（类似 SGLang 的 radix / vLLM 的 hash）。**它证明了"KV 分页 + hash 前缀缓存"是通用做法——你复用 LMCache 就是干这个。**

**⑤ `engine/expert_offload_coordinator.py` — 卸载协调**

```python
def prefetch_experts(self, ...):   # 预取（论文的激活感知预取）
def _handle_expert_fetch(...):     # 从 CPU/盘加载专家
def _handle_expert_evict(...):     # 专家逐出到 CPU/盘
def _target_device_for_expert(expert_id):  # 专家→设备映射
```
**工作栈位置**：这是**专家装载/卸载协调层**。对应 D4 的"装载卸载"。

### 5.4 对我们的启示
- MoE-Infinity 的 **EAM + 优先级打分 + 预取** 是"专家缓存管理"的完整实现，和 SMoE 是同一思路的两派（激活追踪 vs 分数替换）。
- 它的**跨设备 EP（RPC）**展示了 EP 的工程实现，但依赖高速网络——边缘场景需改造传输。
- 它的 `kv_cache_manager` + `scheduler` 表明"KV 分页 + 前缀缓存"和我们的系统是正交组件，可直接用 LMCache 替代。

---

## 六、LMCache（`project/LMCache/`）—— KV 缓存存储与传输

### 6.1 代码在工作栈的位置
LMCache 是**KV 缓存存储与传输层**，vLLM 之上。它在 GPU/CPU/磁盘/远端之间移动 KV，是我们要复用的核心组件（mark01 已验证 WiFi 传输）。

### 6.2 核心接口与文件映射

```
lmcache/
├── v1/                                  # 新一代 API
│   ├── cache_engine.py                  # 缓存引擎入口
│   ├── cache_interface.py               # 缓存接口抽象
│   ├── memory_management.py             # MemoryObj/Allocator
│   ├── storage_backend/                 # ★ 存储后端
│   │   ├── local_cpu_backend.py         #   CPU 热缓存（页锁内存）
│   │   ├── local_disk_backend.py        #   本地磁盘（O_DIRECT）
│   │   ├── gds_backend.py               #   GPU Direct Storage
│   │   ├── remote_backend.py            #   远端后端聚合
│   │   ├── p2p_backend.py               #   P2P 跨实例
│   │   └── connector/                   #   远端连接器
│   │       ├── mooncakestore_connector.py # Mooncake（TCP/RDMA）
│   │       └── redis_connector.py        # Redis/Valkey
│   ├── gpu_connector/gpu_connectors.py  # ★ GPU↔CPU 转换
│   └── transfer_channel/nixl_channel.py # ★ NiXL 传输通道
├── integration/vllm/lmcache_connector_v1.py  # ★ vLLM 集成
├── csrc/mem_kernels.cu                  # CUDA KV 传输核
└── rust/raw_block/                      # Rust 原始块设备
```

### 6.3 核心逻辑

**① KV 传输层次**（`KV-Transfer-Overview.md` 已完整记录）：
- GPU↔CPU：CUDA 核 `multi_layer_kv_transfer` / `single_layer_kv_transfer`（支持 MLA/非 MLA、多种格式）
- CPU↔磁盘：`local_disk_backend`（O_DIRECT）
- 跨实例：`p2p_backend` + NiXL 通道（零拷贝页级传输）
- 远端：Mooncake connector（TCP/RDMA）

**② vLLM 集成**（`integration/vllm/lmcache_connector_v1.py`）：作为 vLLM 的 KV connector 接入，拦截 vLLM 的 KV 存取操作。

**工作栈位置**：LMCache 是我们**KV 存储/传输的执行层**——调度层决定"KV 迁到哪"，LMCache 负责"怎么传"。它的多后端抽象（GPU/CPU/disk/远端/P2P）正好对应你的分级存储需求。

---

## 七、五个仓库对我们的组合价值（总结）

| 我们要做的 | 参考实现 | 具体可复用点 |
|---|---|---|
| 请求路由（KV 命中感知） | **Preble** | E2 exploit/explore + 驱逐价值公式 `PT×N` + radix tree 索引 |
| 节点变动时重并行化 | **SpotServe** | solver 枚举 (D,P,M,B) + KM 匹配 + HotSwitch 层迁移指令 |
| 模型放置 + 在线调度 | **Helix** | FlowGraph 图建模 + IWRR 选择 + KV 水位线 mask + per-request pipeline |
| 专家缓存管理 | **MoE-Infinity** | EAM 追踪 + 优先级打分 + 预取 |
| 专家替换（更高命中） | SMoE（未开源，从论文理解） | 分数感知淘汰 + 保护盾 |
| KV 存储与跨设备传输 | **LMCache** | 多后端 + 传输通道（已用） |
| 管理调度层架构 | **Preble + SpotServe + Helix** | 独立调度层 + 策略接口 + 运行时抽象 + 仿真/真实双实现 |

**整体架构建议**：以 Preble 的"独立调度层 + 策略枚举 + 运行时抽象"为骨架，加上 SpotServe 的"重并行化 + 迁移计划"处理节点变动，用 LMCache 做 KV 传输，用 MoE-Infinity 的优先级打分管专家缓存。**这就是我们要自研的管理调度层的蓝图。**
