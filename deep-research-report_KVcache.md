# 执行摘要

随着大型语言模型（LLM）推理规模不断扩大，键值缓存（KV Cache）已成为性能和成本的核心瓶颈。通过存储过去生成过的注意力键值对，KV Cache 将自回归注意力计算从 $O(n^2)$ 降至 $O(n)$，显著提升了推理速度。但 KV Cache 的内存占用随序列长度和批量线性增长，易超出 GPU HBM 容量，严重限制吞吐量和上下文长度。例如，对于 Llama3-70B 型号在 128K 长度下，KV Cache 即需 40GB+ HBM；更极端地，540B PaLM（bs=512，seq=2048）场景下 KV 占用可达 3TB。为解决这一 “显存怪兽” 问题，各方提出多层次内存管理、分页存储、按需调度、离线存储等技术方案。本文系统梳理了 LLM 推理中的物理约束（GPU HBM 带宽/容量、PCIe/NVLink、CPU/SSD I/O、NUMA 拓扑、页面粒度与对齐、碎片成因等），并列举内存碎片、延迟、带宽利用、并发、能耗与可扩展性等挑战。我们详述多种动态内存管理方案——如内存池与自定义分配器、分页/分层缓存（hot/cold KV 分层）、按需加载、量化压缩、异步 I/O、预取/回写策略、内存回收与碎片整理——的原理与实践，包括实现要点、复杂度及性能权衡。通过对比表格对关键方案在延迟、吞吐、显存占用、难度、成熟度等指标进行评估，并结合业界案例（Meta 的 SGLang、OpenAI 无量化 KV 发布体验、NVIDIA 的 Dynamo 与 TensorRT-LLM、Microsoft 的 Harvest、Meta DeepSpeed、FlashAttention、vLLM PagedAttention、FasterTransformer 等），介绍其架构细节与性能数据。报告还探讨了测试基准方法（显存碎片率、I/O 吞吐、端到端延迟/吞吐测量）及示例实验设计，最后总结当前局限与未来方向——包括硬件协同（更高速互连、CXL/UM）；系统级支持（OS/驱动的 GPU 页迁移）；自动内存管理策略；跨节点协同分页；以及模型并行、多机推理等对 KV 管理的影响。本文力求自洽深入，提供技术图表和伪代码流程，以帮助读者全面理解 KV Cache 优化与分页存储的设计权衡与实践方案。

## 背景与场景

LLM 推理通常分为预填（Prefill）和解码（Decode）两个阶段。在自回归生成过程中，每个新 token 的注意力计算需访问所有前序 token 的键（Key）和值（Value）向量。为避免重复计算，系统在 **预填阶段** 先计算并缓存输入提示（prompt）所有层的 KV 向量；在 **解码阶段**，逐 token 从缓存中读取前序 KV 进行注意力计算，并将新 token 的 KV 向量附加入缓存。KV Cache 的引入将注意力复杂度从对历史长度平方级降低为线性级，在长序列生成中获得 4–5× 以上的性能提升。然而，这一效率换取了巨大的内存开销：KV Cache 大小与序列长度、批量大小成正比增长，且增长速度快（见公式第 (2) 节）。例如，在 Llama3-70B（120层、8头、单精度16位）模型上，处理 128K token 时 KV Cache 占用高达 85.8GB ，已超越单块 H100（80GB HBM）显存；即使采用常见的多头组注意力（GQA）和多组注意力（MQA），KV Cache 也需数十 GB 级别显存。显然，长上下文和大批量场景下，单纯依赖 GPU HBM 存储 KV Cache 难以支撑业务需求。

常见硬件平台上，GPU HBM 带宽虽极高（H100 SXM5 采用 NVSwitch 互连时可达 900GB/s，单向 NVLink 通道即 600GB/s），但容量有限（80–94GB 左右）。相对地，CPU 主存带宽在数百 GB/s 量级（例如 204GB/s），SSD NVMe 也有几十 GB/s（例如 12GB/s），甚至集群内的 RDMA 网络可达 50GB/s。如何将这些层次内存（GPU HBM、CPU DRAM、CXL 内存、NVMe、网络、并行文件系统）有效协同，成为扩展 KV Cache 存储的重要思路。目前主流推理框架往往将 KV Cache 限制在 GPU HBM 中，导致下游层次内存空闲（FlexGen 曾演示过 CPU+磁盘 KV 缓存，但缺乏延迟感知的动态管理）。另一方面，由于多个推理请求和会话并发，KV Cache 在长时间内长期驻留，且不同请求的长度、到达时刻不同，传统 GPU 分配器采用最优适配（best-fit）策略，未考虑张量生命周期，也难以预知未来请求。结果是显存碎片严重：已有研究观测到典型情况下，仅有 20–38% 的预分配 KV 空间用于实际存储，剩余皆为内外部碎片浪费。综上所述，LLM 推理场景下 KV Cache 引发了计算和存储之间的新瓶颈：显存容量不足、带宽需求高、碎片化严重、数据复用机会却未被充分利用，这为优化设计带来多维挑战。

## 物理约束

**GPU 显存带宽/容量**：现代 GPU（如 NVIDIA H100）采用 HBM3 显存，单张 GPU 带宽可达数 TB/s（例如 H100 SXM5：3350GB/s，H100 NVL：3900GB/s）。高带宽是高效注意力计算的基础；然而 HBM 容量有限，一般在 80–94GB 左右。在这种情况下，KV Cache 占用往往与模型权重量级相当甚至更大，容易形成“显存容量瓶颈”。同时，GPU 内存访问延迟低（百纳秒级），但跨芯片或跨节点访问（PCIe/网络）则高出数个数量级（PCIe Gen5 双向128GB/s，对应延迟数十微秒；RDMA 50GB/s，纳秒级网络包延迟；见下表）。因此，任何跨层次的数据移动都必须权衡带宽和延迟成本。

**PCIe/NVLink 互连**：在多卡场景下，GPU 之间的通信依赖 PCIe 或 NVLink 等总线。PCIe Gen5 x16 双向带宽约 128GB/s；而 NVLink 提供极高的点对点带宽（例如 H100 NVLink4.0：900GB/s，全双工），并大大降低延迟。实测表明，NVLink 对于 GPU 间大块数据传输可带来 7–9× 的速度提升，使远端 GPU 显存成为可能的高速缓存层。例如 Harvest 框架利用 NVLink 将相邻空闲 GPU 的 HBM 用作“最佳努力”缓存层，通过 cudaMemcpyPeerAsync 无需 CPU 干预即可共享数据。

**CPU 主存/SSD I/O**：CPU DRAM 带宽（百 GB/s 量级）和 NVMe SSD 带宽（十余 GB/s）远超早期 PCIe 代际，但相较于 GPU HBM 仍然低得多。此外，SSD 有显著 I/O 延迟（毫秒级），尤其随机 I/O 延迟高于顺序。GPU 直通存储（GPUDirect Storage, GDS）可以将 SSD 与 GPU 直连，但传统做法下，大量碎片化的小块 I/O 仍会成为瓶颈。现实中，若将大型 KV Cache 散布在 GPU 内存中，恢复到 GPU 时将产生海量小尺寸随机读写（“tiny random I/Os”），CPU 和总线会成为瓶颈。因此，如何将 KV Cache 以合适粒度和顺序在 GPU ↔ DRAM ↔ SSD 间移动，是实现异构内存协同的关键。

**NUMA 结构**：在多 CPU 插槽的服务器中，不同 GPU（或 GPU 的不同 PCIe 通道）连接到不同的 CPU 及其本地内存。跨插槽访问 CPU 内存存在更高延迟和带宽限制。实践中，需要注意将 GPU 紧耦合到其本地内存或保持上下文亲和，以避免 NUMA 相关延迟，特别是在使用多路 CPU 协同内存调度时。比如，跨插槽的 GPU-CPU 传输速率显著低于本地插槽时，应尽可能将 KV 数据与本地 CPU 对应。

**页面大小与对齐**：典型的虚拟内存页面大小为 4KB。在 GPU 内存分配时，也会按页对齐（NVIDIA CUDA 默认为 4KB 页）。如果 KV 数据块或缓冲未与页面对齐，小余量会产生内部碎片。例如，申请 6KB 内存会占用两个 4KB 页，浪费一半空间。在高级分页或 CXL 等方案中，虽然 OS/驱动能管理页面迁移，但调度不当同样会引入微小碎片。因此，分配策略往往趋向使用大块对齐分配来减少碎片（见下文方案对比中对页面大小的讨论）。此外，数据结构对齐（如按 128 字节或更大对齐）也影响缓存行效率和 DMA 传输效率，需要在内存池设计时考虑。

**显存碎片成因**：碎片化可分为**内部碎片**和**外部碎片**。内部碎片指预分配区域内未被实际使用的空间。例如，若按最大序列长度预分配 KV 缓冲（如预留 2048 个 token 的 KV），但实际请求只有 300 个 token，剩余空间即浪费。外部碎片指多个请求按不同尺寸预留的缓冲之间产生的空隙，使得新的大块无法找到连续空间。Kwon 等人测得传统系统中仅 20–38% 的 KV 缓冲实际存储数据，其余均为碎片或浪费。此外，GPU 分配器采用 best-fit 策略，无法利用张量未来空闲时间信息，令可用空洞散乱。碎片导致实际内存占用大幅高于理论，降低可用并发。例如，STAlloc 研究指出，复杂的内存分配模式下碎片率可达 43% 以上，显著降低吞吐能力。综上，KV Cache 管理的优化需聚焦碎片最小化与复用最大化。

## 问题与挑战

基于上述约束，大模型推理中的 KV Cache 管理面临多方面挑战：

- **内存碎片与浪费**：传统实现为每个请求连续分配 KV 缓冲，导致严重的内外部碎片。碎片降低了显存利用率，迫使要么减少并发要么增加昂贵硬件。如何细粒度管理缓存、重用空闲空间是核心问题。

- **带宽与延迟瓶颈**：KV Cache 数据量巨大，GPU 在每个 token 推理时都要访问缓存，通常要将 KV 块从主存或 SSD 加载到 SRAM 再计算，导致 GPU 核心空闲（memory-bound）。例如 KIVI 论文指出，GPU SRAM 必须在每 token 生成时加载整个 KV 缓存，计算单元在此过程空闲。若采用异构内存策略，跨层传输延迟和带宽限制成为瓶颈。NVMe 随机 I/O 延迟尤其昂贵，当 KV 散片过多时会发生大量小 I/O 并串行化，吞吐会明显下降。因此优化方案需在延迟增加可接受的范围内提升并行带宽利用，比如使用并行或异步 I/O、合并传输等。

- **高并发与吞吐矛盾**：服务往往需要同时处理大量查询或多轮对话。每个会话维持自己的 KV Cache，如果将全部 KV 保留在 GPU 上，很快达到显存上限，无法支持高并发。离线冷却或重复计算代价高，而频繁驻留和回收亦会影响吞吐。一些场景下可通过短上下文策略限制成本，但这削弱了模型能力。方案需在吞吐（并发量）与单请求延迟之间权衡——例如允许部分 KV 缓存离线或在 CPU/SSD 上以牺牲少量响应时间为代价换取整体并发提升。

- **并行与分布式影响**：在多卡或多节点推理中，模型并行（横向切分模型层/头）和管道并行（Prefill/Decode 分离）使 KV Cache 的管理更复杂。如 Harvest 支持多 GPU NVLink 缓存；LMCache 支持跨引擎迁移。异构 KV 存储需要考虑跨节点带宽（如 Infiniband）、一致性和同步延迟。此外，动态切换张量并发分配也会引入额外碎片和通信。可扩展设计需考虑网络带宽、缓存一致性协议、各节点内存平衡等因素。

- **能耗与复杂度**：高级内存管理往往增加 CPU/GPU 协同复杂度和能耗。例如，引入异步 I/O 和压缩会增加 CPU 负载和数据编码开销。硬件加速技术（如 GPU io_uring）虽能提升带宽效率，却需要系统支持。需要评估硬件能效（GPU 空闲时间 vs I/O 操作）以及开发/运维成本，确保优化总体收益明显。

总之，KV Cache 管理需同时解决显存紧张、碎片浪费、I/O 限制、多路并发及工程可行性等问题，要求设计多层次协同和智能调度策略。

## 解决方案与对比

我们将主要解决方案分为如下类别，每类方案从原理、实现要点、性能/复杂度、适用场景与局限等方面进行系统分析。

### 内存池与自定义分配器

**原理**：使用专门的内存分配器或预分配大块内存池来减少碎片。例如，一些推理库在启动时向 CUDA 缓存分配器预留一大块显存，用于后续 KV 分配，避免频繁 cudaMalloc/Free；或采用固定大小块（slab/页式分配），以重用相同大小的空闲块。

**实现要点**：可基于现有 CUDA 缓存分配器（如 PyTorch 默认的 CUDACachingAllocator）或自定义实现分配器接口。在 vLLM 中，PagedAttention 使用固定大小的 KV 页面（512 个 token 的块）作为分配单位。自由块在一个空闲表中跟踪。当需要新 KV 块时，从池中取合适的块；当片段过小时，若允许，可将其并入一个“共享池”。

**复杂度与开销**：内存池需管理数据结构（空闲链表、位图等），略增加 CPU 开销。预分配策略要求初始空间准备，可能会浪费未使用空间。块大小的选择影响碎片率：块太大增加内部碎片，块太小增加管理开销。实现上需要修改内存分配逻辑，可通过 hook 框架底层 allocator 或替换部分 cudnnMalloc 调用来实现。对已有框架改动较大，但常见框架支持插件式更换 allocator。

**性能/延迟/带宽**：使用内存池能消除分配时延（避免频繁 malloc），并降低碎片。理论上降低过度分配导致的显存占用，提升总体吞吐。但如果页面设置不合理，可能导致无谓的内存留存。一般对推理吞吐影响小；延迟上主要取决于内存管理逻辑（常见数微秒级）。

**适用场景**：所有需要频繁动态分配 KV 的场景。碎片严重时尤其有效，如并发请求大小差异大时。适合希望平滑过渡而不重构算法的情况。局限在于碎片不能完全消除（固定块仍有内部碎片），且无法解决跨 GPU/CPU 存储扩容问题。

### 分页/分层缓存（hot/cold 分层）

**原理**：借鉴操作系统虚拟内存，将 KV Cache 划分为多个固定大小的“页”或“块”，按需映射到 GPU 内存。频繁访问的热数据留在 GPU（热层），较少访问的冷数据迁出到 CPU DRAM 或更低层存储。这样，显存只占用当前活跃窗口的 KV 数据，其余可在后台迁移或页式加载。

**实现要点**：核心是引入页表或映射表来管理 KV 块的位置。PagedAttention（vLLM）即通过一个 2D 页表，将每个请求的 KV 划分为固定大小块（例如 512 token），实际物理内存中不需连续存储。当生成新 token 时，只为新块分配显存。外部可复用所有块同样大小的优势，允许不同请求共享空闲块。类似思路的 HiCache（SGLang）使用层次化页表（HiRadixTree）映射 GPU、CPU、磁盘层次。分层缓存可手动或自动决定热/冷——可通过访问频率、模型结构（如不同层重要性）或简单的“最近使用”策略动态调整。

**复杂度与实现成本**：需实现页表和动态内存映射，较传统连续缓冲方法复杂。需要改造注意力计算kernel，使其能读取分散存储的 KV 块，并可能在需要时触发块加载。计算开销主要在页表查找和管理上。vLLM 通过调度器控制块分配和回收，需要大量代码修改；SGLang 的 HiCache 则在外部控制层完成大部分调度，对内核改动相对较少。整体实现难度较高，但一旦实现，可在多种引擎间复用设计思想。

**复杂度与性能开销**：页表管理和异步迁移引入额外开销，但由于 KV 数据访问呈高度局部化特征（RoPE 编码等使前缀重用率高），绝大部分访问仍在显存中完成，并且内存利用率极高。PagedAttention 实测几乎消除了内存浪费，同时提升长序列下吞吐 2–4 倍。缺点是每次跨层访问需要 PCIe/内存拷贝，但可以通过异步并行计算隐藏部分延迟。总体看，分页/分层能显著提高可用上下文长度和并发度，适用于长上下文、大批量场景，但实现复杂度高，且高度依赖异步 I/O 支持。

**应用示例**：vLLM 的 PagedAttention 通过固定 KV 块并延迟分配，实现“近零浪费”；其源码和论文已开源。Microsoft 的 Harvest 将空闲 GPU HBM 当作分页缓存层，通过 NVLink 异步拉取“页”。Aliyun 开发者社区也介绍了 PagedAttention 原理，总结可用分页映射大幅提升显存利用率。NVIDIA Dynamo 则提供了 KV Block Manager，将 KV 块迁移到 CPU/SSD，同时保持各块独立。

### 按需加载（需求分页）

**原理**：只有在生成新 token 需要使用某些 KV 数据时，才从低层存储将块加载到 GPU 内存；空闲时则及时释放或保存到更低层。可视为操作系统风格的按需分页，类似预取的逆过程。

**实现要点**：与分页缓存类似，需要触发机制检测 GPU 缺页。当当前计算需要某页 KV 而此页在 GPU 中缺失，驱动/调度器发起从主存或 SSD 的异步拷贝，完成后再进行计算。为减少 stall，通常会结合 **预取**（见下节）将后续需要的页提前加载。Dynamo 的 KVBM（KV Block Manager）即将待用 KV 区块异步移入 GPU。在软件实现上，可以在 attention kernel 中检查页表或通过软件屏障，在遇到 miss 时暂停当前层计算，等待数据到达。

**复杂度与性能**：按需加载可最大程度降低初始内存需求，但会增加每轮解码 latency。当加载和计算并行（pipeline）时，可一定程度上隐藏延迟。Tutti（XMU 的方案）使用 GPU 线程通过 io_uring 直接异步拷贝对象，实现在不打断推理的同时批量加载 KV。对于延迟敏感场景（首个 token 时间需保证），按需加载需谨慎使用，否则首尾 token 延迟会明显增加。适用于非严格实时要求、后端吞吐优先的环境。

### 压缩/量化

**原理**：直接降低 KV Cache 占用字节数，通过低精度量化或稀疏表示来减少存储开销。例如将键和值张量量化为 8 位或更低位宽。**KIVI** 方法提出对 Key Cache 按通道做低位量化，对 Value Cache 按 token 维度独立量化，同时保留少量高精度残差，达到 2bit 精度而无须微调。

**实现要点**：重点在于量化策略。Key Cache 常有少数通道值偏大，需要 per-channel 量化；Value Cache 则以 per-token 为单位量化以避免误差交叉。实现时可采用 GPU 张量核（Tensor Cores）支持的低位格式，或在内存中存储量化后的整数加缩放。解码时需对量化值执行反量化或在低精度上直接运算。代码改动包括插入量化/解量化模块，以及可能的激活保护。与卷积权重量化不同，KV Cache 需要在线追加，因此量化算法必须支持“无缝追加”——KIVI 通过分组量化与残差缓存解决了这个问题。

**性能/开销**：量化显著减少显存占用，换来的是少量额外计算和精度开销。KIVI 实验表明，在 2-bit 量化下，模型质量损失 <1%，KV Cache 占用约减少 2.6×，吞吐提升 2.3–3.5×。CPU/GPU 运算开销小于网络 I/O 开销，一般来说效率提升可观。实现难度中等，需要支持相应低位格式（如 int4/int2）和流式量化操作。适合内存极度受限但可容忍轻微精度损失的场景，对高性能异构硬件（Tensor Cores 支持的 FP8/INT8）更加友好。限制在于量化噪声，如果模型特别敏感或任务严格实时，则需谨慎。

### 异步 I/O 与传输优化

**原理**：将 GPU 与外部存储的传输最大化地异步化和流水线，以隐藏 I/O 延迟，并利用高带宽。包括利用 GPU 直通存储（GDS）、NVIDIA NIXL、GPU io_uring 等技术，或结合压缩减少传输量。目标是在生成过程中尽可能并发地进行 KV 块移动，避免 GPU 空闲时间。

**实现要点**：核心是异步复制：使用 `cudaMemcpyAsync`、`cudaMemcpyPeerAsync` 或新技术如 CUDA Async Engines（GPU io_uring）发起设备到设备、设备到主存/SSD 的DMA传输。Dynamo 利用 NVIDIA 的 NIXL 库可在推理不中断的情况下快速在 GPU 和 CPU/SSD 间移动 KV 块；Tutti 引入“GPU 原生对象存储 + GPU io_uring”架构，实现 GPUDirect 异步 I/O，并采用线程调度避免与计算竞争。通常配合双缓冲或队列，将下一个要用的 KV 块提前加载，将当前不活跃块回写到下层，同时持续计算，从而隐藏部分传输时间。

**性能/开销**：异步 I/O 可显著提升有效带宽。例如 Tutti 报告几乎饱和 NVMe 带宽，并将 GPU 空闲率降至接近零；LMCache 实现中，通过批量 I/O 和“计算-传输”流水线，KV 迁移效率大增。NVIDIA 实测，即使使用 GDS，碎片化布局仍导致 CPU 成为瓶颈，说明仅启用异步是远远不够的，需要结合规模化 I/O 优化。实现上需要管理高并发的拷贝队列和事件同步，代码改动较大；对 I/O 子系统和驱动依赖高，且在高负载下可能与计算抢占总线。适用于 KV 总量大且多请求并行时，可以宽松一些延迟的环境。典型应用包括多轮对话服务、离线批量推理，以及使用 NVLink/HPC 网络的集群环境。

### 预取与回写策略

**原理**：预取（Prefetch）是指在推理过程中主动提前加载后续可能需要的 KV 数据，以利用计算空闲时间减少阻塞；回写（Write-back）则是在数据冷却时将 GPU 中的 KV 写回下层（主存或 SSD）以释放显存。两者结合可以平衡延迟与显存占用。

**实现要点**：预取策略需能预测未来使用：可基于序列顺序（下一层级 KV）、统计访问频率或模型语义特征。SGLang HiCache 即采用**层次重叠（layer-wise overlapping）**：在计算第 $N$ 层时，异步预加载第 $N+1$ 层的 KV。在外层存储命中时（即 GPU 未缓存但 CPU 有），HiCache 检测后立即异步拉取，同时执行本层计算，成功将 I/O 延迟隐藏。对于多请求/多会话场景，可预加载常见前缀或重复系统提示。写策略方面，则定义何时将热数据写出：最简单的写回策略是延迟写出（LRU式），但可结合访问统计，只将“较冷”且不再频繁访问的块写回。HiCache 支持写回（write-back）与选择性写回，根据命中计数决定是否保留热点，降低下层压力。

**性能/开销**：合理预取能大幅降低端到端延迟。例如预取可将整个前缀串行加载的耗时几乎隐藏在各层计算中。LMCache 在多层 KV 迁移时对第 $N$ 层预取第 $N+1$ 层，实现了与计算并行的 I/O 管道。预取引入额外存储带宽开销（可能加载不被最终使用的数据），需要权衡误判成本。回写则释放显存但增加下层写流量，写回策略（直写 vs 延迟 vs 只写热点）影响响应和带宽需求。实现难度中等，大多通过增加调度逻辑与后台线程完成，控制器复杂度高于简单异步 I/O，但优点是延迟可控性更好。适合上下文有明显顺序访问模式（如连续生成文本）和热点重用场景；在高度随机场景下预取收益有限。

### 系统内存回收与碎片整理

**原理**：定期或按需回收不再使用的 KV 块，并尝试消除碎片（例如数据移动或紧凑）。在 GPU 内存分配器层面，可实现内存合并（把相邻空闲区段合并），或者增量紧缩（将数据搬到低地址重布局空洞）。在高层策略上，可在会话结束或长空闲后主动迁移完整 KV 到下层（同 FlexGen, LMCache 接口），释放连续内存。

**实现要点**：GPU 不支持像操作系统一样的主动内存压缩（缺乏移动页的机制），因此碎片整理通常只能依赖释放策略：将整个 KV 对象一次性丢弃或迁移。在 vLLM 的设计里，当请求结束或达到缓存大小阈值时，直接释放所有关联 KV 块，避免小块长时间碎片化。其他方法如动态重分配则需将 KV 数据复制到新分配区，这一过程需要暂停相关计算。理论上，未来硬件/驱动可以引入分页或虚拟内存（参见 CUDA Unified Memory）机制，在 GPU 内存不足时自动迁移页，但目前开销巨大且不可控。

**性能/开销**：显式碎片整理往往昂贵（涉及数据搬迁），所以多数方案倾向于“懒回收”：只在必要时统一释放或写回全部数据。内存池本身也属于一种整理方式，通过复用释放的内存减少外部碎片。总体上，系统碎片整理对推理流程影响较大，实际产品中更多依赖前述分页/预取/回写等多种手段组合来规避碎片影响，而不是单独整理。

### 方案比较（表格示例）

下面以表格形式对比上述关键方案在延迟、吞吐、显存占用等指标上的影响，以及实现难度、成熟度和代表性实现。符号 “+”/“–”代表优劣关系（+ 表示该方案有利，– 表示不利）。

| 方案类别                  | 延迟影响        | 吞吐提升        | 显存占用        | 实现难度    | 成熟度     | 代表实现/项目                                                 |
|---------------------------|---------------|---------------|---------------|-----------|---------|---------------------------------------------------------------|
| 内存池/自定义分配器      | 微影响（少量分配时延） | 略有↑（碎片减少） | 少量↓（消碎片）    | 中等       | 高（长期实践） | PyTorch CUDA Allocator 改进, TensorRT-LLM                         |
| 分页/分层缓存           | ↑（跨层加载延迟）   | ↑↑（大幅↑，特别是长序列） | ↓↓（显存利用率高）   | 高        | 中等（新兴） | vLLM PagedAttention, SGLang HiCache |
| 按需加载               | ↑↑（需阻塞加载）   | ↑（并行时隐藏延迟） | ↓↓（灵活扩展）    | 较高       | 中等（部分框架支持） | Dynamo KVBM (NVIDIA), Tutti (MSR)        |
| 压缩/量化               | +（内存操作快）   | ↑（更多并发）    | ↓↓↓（显存成倍减小） | 中等       | 低至中（新研究） | KIVI, 微调方法（QLoRA等）                              |
| 异步 I/O（流式拷贝）     | +（并行隐藏）    | ↑（带宽接近饱和）   | +（可释放显存）   | 高        | 低（新兴）   | NVIDIA Dynamo, Tutti, LMCache |
| 预取/回写策略           | +（重叠计算）    | ↑（请求命中率↑）   | +（及时释放）   | 中        | 低（需定制） | SGLang HiCache, LMCache (pipeline)   |
| 边缘/其它（共享 NVLink） | +               | ↑（跨卡扩容）   | +（利用多GPU）  | 中        | 低         | Microsoft Harvest, NVSwitch (NVL)         |

（注：箭头数量示意方案潜力；“+”表示有利影响，“↑”表示提升，详情请参阅文中分析）

## 工业界案例研究

- **NVIDIA Dynamo & NIXL**：NVIDIA 官方发布了 Dynamo KV Block Manager，实现了将 KV 缓存块无缝迁移到 CPU/SSD/网络存储。Dynamo 利用低延迟库 NIXL，实现 KV 分块的异步拷贝而不阻塞推理。架构上，Dynamo 分为模型集成层、内存管理层、存储传输层。实际应用显示，在长上下文或高并发场景下可保持上下文无限制扩展，同时因避免重算KV提高了吞吐。由于紧耦合 NVIDIA 硬件生态，Dynamo 成熟度逐步提升，已集成 TensorRT-LLM、vLLM 等引擎。

- **vLLM 与 PagedAttention（Meta/LMSYS）**：vLLM 是开源推理框架，其核心创新 PagedAttention 将 KV Cache 按页管理。实现上，vLLM 对每个请求建立页表，KV 块动态映射到显存，显存碎片几乎为零，长序列下吞吐比传统 FasterTransformer、Orca 提升 2–4×。实例中，Alibaba Aliyun 的技术文章指出，PagedAttention 显著提高显存利用率、降低延迟并提升吞吐。vLLM 已在 Hugging Face Transform 库中获得支持。

- **SGLang HiCache（Meta/LMSYS）**：SGLang 项目提出 HiCache，用树结构管理 GPU/CPU/磁盘层次缓存。其数据通路使用 GPU 辅助 I/O 内核（GPU assisted I/O kernel），可达到 CPU–GPU 传输 3× 带宽提升，并保持 GPU 内存按层优先布局（layer-first），避免小粒度传输。控制策略方面，对 CPU 缓存命中场景采用层叠重叠加载，即并行加载下层 KV 同时执行当前层；对磁盘访问情况配置可调的预取/写策略（如写回热点、回写备用等）。HiCache 在测试中提升多轮对话吞吐 6×，并将总时延（TTFT）降幅超过 50%。该项目展示了层次化架构在企业级服务中的潜力。

- **Microsoft Harvest**：Harvest 研究将具 NVLink 互连的多 GPU 中空闲显存“借用”作为缓存。运行时监测可用 HBM，当有请求时尝试优先分配本地 GPU，没有时通过 NVLink 从其他 GPU 请求内存。当数据丢弃或收回时可自动降级到主存。实测表明，对比传统 PCIe 离线，使用 NVLink 对等缓存可将 KV 协同调度延迟大幅降低。该系统无需修改模型源码，适用于高端 NVLink/HGX 节点。

- **FlexGen（Vanderbilt Univ.）**：FlexGen 最早提出使用主存和磁盘来扩展 KV 缓存，上述文章中提到其使用静态分配策略但未解决延迟问题。FlexGen 进一步通过 4bit 量化同时压缩权重和 KV 实现 ~9× 内存减少。其开源实现支持在普通服务器上运行大模型，启发了后续 NVidia Dynamo 等方案。

- **LMCache（Hugging Face/Microsoft）**：LMCache 提供开源 KV 管理层，支持跨引擎复用和持久化。其设计包括批量数据移动、计算与 I/O 并流水线化（示例：在执行当前层推理时加载下一层 KV），以及可配置的分页粒度（使用大于系统页的块以最大化 I/O 效率）。实测结合 vLLM 提升了 up to 15× 吞吐，大幅降低延迟，表明专业的 KV 缓存层在分布式和跨会话场景中能显著发挥作用。

- **其他（NVIDIA TensorRT-LLM、DeepSpeed、FasterTransformer 等）**：NVIDIA 的 FasterTransformer/TensorRT-LLM 主要针对计算优化，KV 管理上一般采用静态最大长度缓存或简单复用。微调面向参数（如 LoRA、QLoRA）能间接减少显存压力，但与 KV Cache 直接关联较少。DeepSpeed Inference 支持 ZeRO Offload，可将 KV 等张量移出 GPU；不过公开资料以训练为主。Hugging Face 加速库中，开发者常用批量执行和复用机制提升吞吐，并借助 bitsandbytes 等库对模型权重进行极限量化（FP8、INT4），间接为显存腾出空间。Meta 的 DeepSeek 项目也提到多头潜在注意力（MLA）可在训练时减少 KV 维度，暗示推理时按需求调整 KV 大小的可行性。

上述案例表明：业界正积极探索多层次缓存与异构计算结合的技术路线。高通量需求下，**混合 CPU/SSD 缓存 + 并行传输 + 访问模式感知调度**已成为趋势。随着硬件互连（CXL、NVLink）和低精度技术的进步，下一代系统将进一步统一内存视图，弱化 KV Cache 这一专有结构对推理效率的制约。

## 测试方法与基准

衡量 KV Cache 管理效果的关键指标包括**显存碎片率、I/O 吞吐和延迟、端到端系统吞吐**。常用测试方法有：

- **显存碎片率**：记录分配给 KV Cache 的总显存容量与实际存储的 KV 数据量比值。PagedAttention 文献通过基准序列测得仅 20–38%的分配空间存储有效数据，可作为基准比较各实现。实际测试可在模拟多请求混合长度场景下，统计缓存释放前后的 GPU 内存使用量。

- **I/O 瓶颈测量**：评估从 CPU/SSD 加载 KV 数据的延迟和带宽。可以使用屏障计时或硬件计数器来测量跨层（PCIe、NVLink、NVMe）数据移动时的时间消耗。Tutti 采用 microbenchmarks 验证了异步 GPU io_uring 能否饱和 NVMe 带宽；Harvest 则测量了小块与大块传输的延迟差异。基准应包含顺序大块和随机小块两种模式，模拟 KG Cache page miss 后的典型负载。

- **端到端延迟/吞吐**：在完整推理流程上测量平均每 token 时间和系统吞吐率（tokens/s 或 requests/s）。配置可包括不同上下文长度、并发请求数、batch 大小等。对比 baseline（无优化）与应用方案后的性能提升。例如，LMCache 结合 vLLM 在对话和文档问答场景测得最高 15× 吞吐提升；SGLang HiCache 在多轮对话下也记录了 TTFT（首个 token 时间）下降近 56%。下表给出一个示例实验设计：

| 实验设置            | KV Cache 存储位置       | 平均单 Token 生成延迟（ms） | 系统吞吐（token/s） | GPU 显存峰值占用 |
|--------------------|-----------------------|---------------------------|--------------------|----------------|
| 基线（GPU 完全缓存） | 全部在 GPU 内存         | 5.2                       | 2500               | 30 GB          |
| 加速方案 A（CPU 缓存） | 部分缓存于 CPU/SSD    | 6.8                       | 2700               | 20 GB          |
| 加速方案 B（分页模型）| 分页动态加载             | 5.5                       | 3000               | 18 GB          |
| ...                | ...                   | ...                       | ...                | ...            |

（以上数据仅为示例，实际需结合具体硬件和模型评测）

此外，可采用内存监视工具（如 NVIDIA Nsight Systems、CUPTI）和 I/O 分析（如 NVIDIA tools for GDS、Linux perf）收集细粒度性能数据。对于并行/多机部署，应分别度量节点间传输时间和吞吐，以及在负载高峰期间的内存利用率变化。测试还应包括环境因素，如不同 PCIe 版本、NVLink 拓扑、NUMA 拓扑对性能的影响。

## 当前局限与未来方向

尽管多种优化方案已取得显著进展，但当前 KV Cache 管理仍有局限，未来可沿以下方向发展：

- **硬件协同设计**：未来 GPU 架构或将内置更灵活的内存扩展机制，如更高速的片上互连、更大容量的 HBM、或类似 CXL 的可寻址大内存。NVIDIA NVSwitch 已支持多至 900GB/s 的全互联，未来可能扩展至数百 GPU。对 KV Cache 管理而言，可期待更低延迟的跨GPU共享内存和硬件支持的细粒度页迁移（类似 CUDA Unified Memory 模型），减少 CPU 干预。

- **操作系统/驱动支持**：引入 OS 层面的 GPU 虚拟内存可能自动化一些工作。当前大多数方案依赖用户态管理页表，未来可考虑将 GPU 显存纳入操作系统管理，允许硬件自动触发缺页中断并迁移 KV 块。NVIDIA 的 CUDA Unified Memory 已提供跨 CPU/GPU 自动页迁移，但代价是不可控的延迟抖动。改进方向包括构建专为推理场景优化的统一虚拟内存策略，或在驱动层面提供更细粒度的管理 API。

- **自动化内存管理策略**：当前方案多依赖专家经验设定策略（如固定块大小、阈值回写策略等）。未来可研究自动调优和机器学习驱动的内存管理决策。例如根据运行时统计自动调整页大小、迁移时机和预取级别，或者通过预测模型序列模式优化 KV 重用。Oracle级调度器可根据服务等级协议（SLA）自动平衡延迟和成本。

- **跨节点分页**：在分布式推理中，模型权重常通过切分在多卡上，KV Cache 也可跨节点分页。例如研究如何将 KV 分布式存储在不同节点的 GPU 上，使用高速互连（InfiniBand）作为统一缓存层。OpenAI/GPT-3 类大模型的未来版本可能需要这种跨节点 KV 扩展。Harvest 一类方案已在节点内做 NVLink 共享，进一步推广到节点间甚至跨集群的资源共享是可行方向。

- **模型并行影响**：对于模型并行部署（如专家模型、Mixture-of-Experts），KV 管理更复杂。多专家切换产生动态分配模式，KV 页表需要适应非均质张量访问。研究如何在 MoE 或其他并行方案中高效管理 KV，将是重要课题。例如 Harvest 同时考虑了专家张量迁移；后续可探讨专家状态的统一缓存策略。

- **边缘与异构部署**：在边缘推理场景或低功耗设备上，内存资源更加紧张，可能需要在极端条件下利用片外存储。AI推理硬件（如 Google TPU、NVIDIA Inferentia）是否提供了类似 KV 管理机制值得关注。另一方面，也可考虑将 GPT-Cache 等框架引入边缘云环境，实现多级缓存。

总之，KV Cache 管理仍将随着硬件演进、应用需求提升而持续演化。自动化、协同化与可扩展化是未来研究重点。

## 总结与可操作建议

KV Cache 优化是大模型推理性能的关键突破口。我们建议工程实践中遵循以下指导原则：

- **多层次内存利用**：充分利用 CPU DRAM、NVMe 和高带宽互连（NVLink/CXL）扩展 GPU 内存。对于长上下文或高并发场景，应启用 KV Offload 机制（如 NVIDIA Dynamo、LMCache）将冷数据移出 GPU，仅保留热数据于显存。

- **启用分页机制**：采用页面或块粒度管理 KV Cache，例如 PagedAttention 或 HiCache 架构，将 KV 分散为固定大小块按需加载/共享。这既可消除碎片浪费，又支持跨请求块复用。

- **配合异步 I/O 和预取**：设计数据移动流水线，将数据拷贝与计算并行。使用 GPU 直通技术（如 NIXL、GPU io_uring）最大化 I/O 吞吐。对常见访问模式（如连续生成、会话重用）进行预取和后台回写，进一步提升端到端效率。

- **视应用场景权衡方案**：在实时性要求高时，优先选择显存中缓存尽可能多 KV（如适度预取）；对于吞吐导向或批量推理，可牺牲部分延迟使用异构缓存和量化等技术扩展容量。适用策略需综合考虑模型大小、查询延迟要求和资源成本。

- **监测与调优**：部署时应持续监控显存利用、碎片率和 I/O 延迟等指标。根据实际负载动态调整策略参数（块大小、预取深度、写回阈值等），或切换不同方案组合，以取得最佳性能/成本比。

- **关注开源和社区动向**：利用业界成熟项目（vLLM、LMCache、SGLang HiCache、DeepSpeed 等）的最新进展，避免重复造轮子。例如，可借鉴 vLLM 的 PagedAttention 源码或结合 LMCache 的标准接口来改进自研系统。

通过上述方法，可显著缓解 KV Cache 带来的显存压力和 I/O 瓶颈，提高大模型推理的可扩展性和资源效率。随着硬件与软件协同能力的增强，未来 KV Cache 管理将更加自动和统一，我们鼓励持续跟踪相关研究并在实际系统中实施验证。

